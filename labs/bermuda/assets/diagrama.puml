@startuml

skinparam defaultFontName "Source Code Pro"
skinparam dpi 150

class Bermuda {
  {static} + DEFAULT_SIZE: string
  {static} - ALLOWED_SIZES: Array<string>
  - size: string
  __
  + Bermuda(size: string)
  - validateSize(size: string): void
  + getSize(): string
  + setSize(size: string): void
  + getAllowedSizes(): Array<string>
  + toString(): string
}

note right of Bermuda::validateSize
  Regra compartilhada pelo construtor
  e por setSize.
end note

class Shell {
  __
  + main(): void
}

Shell ..> Bermuda : usa

@enduml
