@startuml

skinparam defaultFontName "Source Code Pro"
skinparam dpi 150

class Towel {
    + color: string
    + size: string
    + wetness: number
    __
    + constructor(color: string, size: string)
    + wringOut(): void
    + getMaxWetness(): number
    + dry(amount: number): boolean
    + isDry(): boolean
    + toString(): string
}
@enduml
