#include "fn.hpp"
struct Student {
    // Car car;
    Student() {}

    void enter() {
        // car.enter();
    }
    void leave() {
        // car.leave();
    }
    void fuel(int q) {
        // car.fuel(q);
        (void) q;
    }
    void drive(int q) {
        // car.drive(q);
        (void) q;
    }
    void show() {
        // std::cout << car.str() << std::endl;
    }
};
