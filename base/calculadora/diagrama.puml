@startuml

skinparam defaultFontName Hasklig
skinparam dpi 150

class Calculator {

  + battery    : int
  + batteryMax : int
  + display    : float
  __

  + Calculator(batteryMax : int)
  + charge(value : int)
  + sum(a : int, b : int)
  + division(num : int, den : int)
  + toString(): string
}

class Legenda {
  + atributoPublic
  - atributoPrivate
  # atributoProtected
  __
  + métodoPublic()
  - métodoPrivate()
  # métodoProtected()
}

@enduml
